# Godot 4+ specific ignores
.godot/
/android/

# Native GDExtension: binarios e intermedios de build (no versionar)
native/ocean_query/bin/
native/godot-cpp/
# SCons offline extraído del wheel (dependencia de build, no versionar).
native/deps/
# Descriptor ACTIVO generado por el build (el template sí se versiona).
native/ocean_query/water_race_ocean_query.gdextension
# Archivo de godot-cpp descargado para preparar el build (no versionar).
/godot-cpp-master.zip
